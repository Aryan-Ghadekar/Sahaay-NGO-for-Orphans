# Base URL of the backend API. Leave unset during frontend-only development —
# src/api/client.js falls back to bundled mock data when this is empty.
VITE_API_BASE_URL=

# Set to "true" to force mock data even if VITE_API_BASE_URL is set (useful for demos).
VITE_USE_MOCKS=true
